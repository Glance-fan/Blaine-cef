/*global*/
- новый template: Tuning
- из index вынесены стили в файл main.css

/*inventory*/
- в tooltip убрано каждое слово с заглавной буквы

//trade
- исправлен баг галок
- изменено расстояние имуществ

/*shop*/
- в jsdb добавлены разделы

/*aciton-box*/
- к именам больше не дописываются #id
- удален capitalize

/*menu-home*/ + /*reatil*/
- подключены Картинки мебели 

/*menu-home*/
- теперь при отрисовке интерфейса выбирается раздел Информации
- теперь все предыдущие действия в разделах игрока сохраняются
- изменены callback функции тоггликов в соответствии с требованиями
- добавлен (бесполезный) зеленый цвет кнопки 

//layout
- изменен размер кнопок покупки на размеры фигмы,
а также добавлены отступы от кнопок

//roommates:
- к именам больше не дописываются #id
- если сожителей >0, то при открытии выбирается первый сожитель
-из функции newRoommate(parent, id, name, permits) удален передаваемый параметр parent
также теперь при добавлении нового сожителя он автоматически выбирается
- добавлена функция removeRommate(id); при удалении выбирается первый сожитель (либо никакой)
- добавлена функция setPermit(id, state, index)

//furniture
- в функции newRoommate(parent, uid, id, name) передаваемый параметр parent заменен на which = 'installed' || 'possible', также теперь при добавлении новой мебели она автоматически выбирается
- добавлена функция removeFurniture(which, id); при удалении также выбирается первая мебель переданного контейнера
- передаваемые данные изменены на: [installed_container, possible_container, max_furniture]

//light 
- теперь предыдущие выбраные цвета сохраняются, а не перерисовываются на переданные
- теперь выбирается первый источник света (если есть)
- теперь при нажатии на применить никогда не возвращается null